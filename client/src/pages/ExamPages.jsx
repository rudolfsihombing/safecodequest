import React from 'react'
import ExamBody from '../components/ExamBody'

const ExamPages = ({token}) => {
    return (
    <div>
    <ExamBody token={token}/>
    </div>
    )
}

export default ExamPages
